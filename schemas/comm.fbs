namespace midi2pwm.comm;

table Ping {
  sequence: uint;
  timestamp: ulong;
}

table ControlChange {
  channel: ubyte;
  controller: ubyte;
  value: ubyte;
}

union Packet {
  Ping,
  ControlChange
}

table Envelope {
  packet: Packet;
}

root_type Envelope;
file_identifier "M2PW";
